{
  "name": "lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for my CDK app",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "node index.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "aws-lambda": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  },
  "engines": {
    "node": ">=14.x"
  }
}